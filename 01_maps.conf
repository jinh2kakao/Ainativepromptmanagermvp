map $http_origin $cors_origin_header {
    default "*";
    "~^https?://(localhost|promptlib\.co\.kr|www\.promptlib\.co\.kr)$" "$http_origin";
}

map $request_method $cors_method_header {
    default "GET, POST, OPTIONS, PUT, DELETE, PATCH";
}
