# [v3.6.0] Growth Onboarding Implementation Plan

## 1. Backend Implementation (FastAPI)

### 1.1 Router Setup
- [ ] Create `backend/routers/onboarding.py`.
- [ ] Define `GET /api/onboarding/templates`:
    -   Logic: Fetch top 3 templates by category (hardcoded IDs or `is_featured` flag initially).
- [ ] Register router in `backend/main.py` with prefix `/api/onboarding`.
- [ ] Update `backend/core/config.py` (if needed) to allow unauthenticated access to these endpoints.

### 1.2 Data & Models
- [ ] Ensure `seed_templates.py` has high-quality templates for:
    -   Coding (React, Python refactoring)
    -   Marketing (Blog post, Email)
    -   Business (Report info, Summary)
    -   Study (Translation, Quiz)

---

## 2. Frontend Implementation (Next.js)

### 2.1 Layout & Structure
- [ ] Create `src/app/(onboarding)/layout.tsx`:
    -   Remove `Sidebar`, `Navbar`, `Footer`.
    -   Add `ProgressBar` component (Step 1 to 5).
- [ ] Create `src/stores/onboardingStore.ts`:
    -   State: `step`, `selectedCategory`, `selectedTemplate`, `userInputs`.

### 2.2 Page Implementation
- [ ] **Step 1 (Welcome):** `src/app/(onboarding)/welcome/page.tsx`
    -   Hero section, Lottie animation, "Start" button.
- [ ] **Step 2 (Persona):** `src/app/(onboarding)/persona/page.tsx`
    -   Card selection grid. Save category to store -> push to Step 3.
- [ ] **Step 3 & 4 (Magic):** `src/app/(onboarding)/magic/page.tsx`
    -   Fetch templates using React Query.
    -   Template selection UI.
    -   Simplified input form (dynamic based on template variables).
    -   Mock "Generating..." animation.
- [ ] **Step 5 (Result):** `src/app/(onboarding)/result/page.tsx`
    -   Display generated prompt.
    -   "Copy" button (functionality + toast).
    -   "Save" button (trigger standard Auth Modal).

### 2.3 Middleware & Auth
- [ ] Update `middleware.ts`:
    -   Exclude `/onboarding/*` from protected routes.

---

## 3. Verification & Deploy
- [ ] **Local Testing:**
    -   Run flow as guest.
    -   Verify API responses.
    -   Check mobile responsiveness.
- [ ] **Deployment:**
    -   Push to `main`.
    -   Verify on Cloudflare/EC2.
