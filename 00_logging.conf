log_format debug_cors '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_origin" "$http_access_control_request_method" '
                      'resp_origin:"$sent_http_access_control_allow_origin"';
